# 2026-07-07 SDD-Flow 專案設定修正記錄

## 修正項目（已合併到 main）

### 1. agents-loader.json
- **問題**: 多餘的 `command` 欄位 + 不一致縮排
- **修正**: 移除 `command: echo '{"continue":true}'` 行，統一縮排

### 2. copilot-instructions.md
- **問題**: 與 AGENTS.md 規則重複（決策前強制詢問、AGENTS.md 載入等）
- **修正**: 移除重複規則，僅保留「規則來源」說明（VS Code + Copilot 專用）

### 3. rtk_settings.json
- **問題**: 內容與檔名不符（原為 Claude Code 設定）
- **修正**: 更正為 VS Code + Copilot RTK hook 設定，matcher 為 `run_in_terminal`

### 4. utf8-check.py
- **問題**: macOS/Linux 上邏輯無效
- **修正**: 簡化為直接放行（macOS/Linux 終端已預設 UTF-8）

### 5. check-speckit-update.yml
- **問題**: 未使用的 `CHANGED` 變數指派
- **修正**: 移除該行

### 6. Docs/History/2026-07-05_SDD-Flow專案優化規劃.md
- **問題**: Phase 編號跳號（4→6）
- **修正**: 修正為連續編號

## 重要決策記錄

- `.copilotignore` 不應簡化為僅保留與 `.gitignore` 不同的部分，兩者目的不同
- `rtk_settings.json` 命名已改為 VS Code + Copilot 專用
- AGENTS.md 為通用規則 source of truth，copilot-instructions.md 僅保留 Copilot 特有設定
