---
name: python-venv-check
description: 在執行 Python 專案開發、相依套件相關指令、測試或套件安裝前，檢查並沿用專案根目錄的 .venv；若不存在，才詢問要建立環境或使用系統 Python。若 repository 工具所屬的 skill 或文件已明確聲明只使用 Python 標準函式庫、免 venv 與安裝，則不觸發此技能。
---

# Python 虛擬環境檢查流程（.venv 優先）

此技能用於標準化有相依套件需求的 Python 專案執行流程，避免重複建立虛擬環境，並遵循「先用現有、再詢問」原則。

## 適用情境

- 執行 Python 專案開發、測試、套件安裝，或依賴專案／第三方套件的指令前
- 專案可能已經有既存 `.venv`
- 需要在建立新環境前先與使用者確認，若已經有確認有 `.venv` 環境則直接使用

## 例外：自包含的 Repository 工具

若工具所屬的 skill 或文件已明確聲明以下兩點，不得觸發此技能：

- 只使用 Python 標準函式庫；且
- 不需要 venv 或套件安裝。

直接以文件指定的系統 Python 指令執行，不檢查 `.venv`，也不詢問環境選擇。若系統沒有 Python 直譯器，回報缺少必要條件；建立 venv 無法提供 Python 本身。

## 核心原則

1. 先判斷指令類型；所屬文件已明確允許時，套用自包含工具例外
2. 其他情況才檢查專案根目錄是否存在 `.venv`
3. 若 `.venv` 存在：直接使用該環境，不建立新的虛擬環境
4. 若 `.venv` 不存在：先使用 VS Code `vscode/askQuestions` 工具，並設定 `allowFreeformInput: true`
   - 是否要建立新的虛擬環境
   - 或改用系統預設 Python 環境
5. 未取得使用者明確選擇前，不主動建立新虛擬環境

## 標準流程

### 步驟 0：檢查是否符合文件化例外

若工具符合自包含的 repository 工具例外，直接依文件執行並結束本流程。

### 步驟 1：檢查 `.venv` 是否存在

以目前專案根目錄為準檢查 `.venv` 目錄。

### 步驟 2：若存在 `.venv`

- 視為首選 Python 執行環境
- 後續 Python 指令與套件安裝都應使用此環境
- 不再詢問是否建立新環境（除非使用者主動要求）

### 步驟 3：若不存在 `.venv`

向使用者提出二選一：

- 建立新的虛擬環境（例如 `.venv`）
- 使用系統預設 Python 環境

範例詢問句：

「目前在專案根目錄找不到 `.venv`。你希望我現在建立一個新的虛擬環境，還是改用系統預設 Python 環境？」

## 實作提醒

- 以專案根目錄為唯一判斷基準，避免在子目錄重複建立環境
- 若已存在 `.venv`，不要覆寫或重建
- 記錄使用者選擇並在同一工作流程中保持一致
- 若使用者後續改變選擇，再切換環境策略

## 完成條件

- 符合例外的 repository 工具已直接執行，未重複檢查 `.venv`；或
- 已完成 `.venv` 檢查，並採用既有環境或取得使用者明確選擇
- 後續有相依套件需求的操作皆使用一致的 Python 環境
